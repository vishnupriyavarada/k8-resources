apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs-static-pvc
spec:
  storageClassName: ""
  volumeName: ebs-static # pv name should be given here
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

